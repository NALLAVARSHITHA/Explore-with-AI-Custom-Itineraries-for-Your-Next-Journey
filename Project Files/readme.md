travel.py is the main code to execute this first install required libraries using the command
pip install -r requirements.txt
run the code using the code
streamlit run travel.py
The GOOGLE_API_KEY is stored in .env in the project folder
